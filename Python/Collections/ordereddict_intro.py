# older python version now it is not used much
from collections import OrderedDict
from collections import defaultdict
ordered_dict = OrderedDict()

df_dict = defaultdict(int)
# so you can set a default value that can be entered in it and if the entered value doesn't match then it takes default value

from collections import deque

# same as list
d = deque()
d.extend()
d.extendleft()
d.rotate(1)
d.rotate(-1)
d.append(1)

